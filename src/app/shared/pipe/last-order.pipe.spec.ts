import { LastOrderPipe } from './last-order.pipe';

describe('LastOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new LastOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
